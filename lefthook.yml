pre-commit:
  jobs:
    - name: ktfmt
      run: ktfmt --google-style {staged_files}
      glob: 'android/**/*.kt'
      stage_fixed: true
    - name: swiftformat
      run: swiftformat {staged_files}
      glob: 'ios/**/*.swift'
      stage_fixed: true
